import React from 'react';

import { HomeScreen } from '@/screens';

/**
 * Root navigator placeholder.
 * Replace with @react-navigation/native stack when navigation library is added.
 */
export const RootNavigator: React.FC = () => {
  return <HomeScreen />;
};
